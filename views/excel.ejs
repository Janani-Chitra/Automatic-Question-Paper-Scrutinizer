<html>

<head>
    <title>Automatic Question Paper Scrutinizer</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <script src="/js/jquery.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
    <script src="/js/angular.min.js"></script>
</head>

<body>
    <nav class="navbar navbar-info bg-info">
        <div class="container">
            <span class="navbar-brand mb-0 h1">Automatic Question Paper Scrutinizer</span>
        </div>
    </nav>
    <form action="/download" method="POST" class="container">
        <div class="form-group">
            <label>Curriculum</label>
            <select class="form-control" name="curriculum">
                <% rows.forEach(row=>{ %>
                <option>
                    <%- row.cur_id %>
                </option>
                <% }) %>
            </select>
        </div>
        <!-- <div class="form-group">
            <label>Semester</label>
            <select class="form-control">
                <% rows.forEach(row=>{ %>
                <option>
                    <%- row.semester %>
                </option>
                <% }) %>
            </select>
        </div>
        <div class="form-group">
            <label>Course ID</label>
            <select class="form-control">
                <% rows.forEach(row=>{ %>
                <option>
                    <%- row.course_id %>
                </option>
                <% }) %>
            </select>
        </div> -->
        <div class="form-group">
            <label>Paper ID</label>
            <select class="form-control" name="paper_id">
                <% rows.forEach(row=>{ %>
                <option>
                    <%- row.paper_id %>
                </option>
                <% }) %>
            </select>
        </div>
        <button type="submit" class="btn btn-outline-info">Download</button>
    </form>
</body>

</html>